Grammar:	Map 5.1
Namespace:	cimi.composition
Target:		FHIR_STU_3

//InformationEntry maps to Resource:

Composition maps to Composition:
    Section maps to section
    Section.InformationEntry maps to section.entry  (slice on = reference.resolve(); slice on type = profile; slice strategy = includes)
   
    
    // (slice on = coding.code; slice strategy = includes)